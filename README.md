# LMS Frontend

Современный веб-интерфейс для системы обучения (LMS) с автоматической генерацией конспектов и тестов.

## Технологический стек

- **Framework**: Vue 3 (Composition API с `<script setup>`)
- **Build Tool**: Vite
- **Language**: TypeScript
- **Styling**: Tailwind CSS
- **State Management**: Pinia
- **Routing**: Vue Router
- **HTTP Client**: Axios
- **UI Icons**: Heroicons / Emoji

## Структура проекта

```
frontend/
├── public/              # Статические файлы
├── src/
│   ├── assets/         # Стили и ресурсы
│   ├── components/     # Компоненты
│   │   ├── common/    # Переиспользуемые UI компоненты
│   │   ├── layout/    # Компоненты layout
│   │   ├── methodist/ # Компоненты для методиста
│   │   └── student/   # Компоненты для студента
│   ├── composables/   # Vue composables
│   ├── layouts/       # Layouts для роутинга
│   ├── router/        # Конфигурация Vue Router
│   ├── stores/        # Pinia stores
│   ├── types/         # TypeScript типы
│   ├── views/         # Страницы приложения
│   ├── App.vue        # Корневой компонент
│   └── main.ts        # Точка входа
├── .env.development   # ENV переменные для dev
├── .env.production    # ENV переменные для prod
└── package.json
```

## Установка и запуск

### Предварительные требования

- Node.js >= 18
- npm или yarn
- Backend API должен быть запущен на `http://localhost:8000`

### Установка зависимостей

```bash
cd frontend
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Preview production сборки

```bash
npm run preview
```

## Переменные окружения

Создайте файл `.env.development` для локальной разработки:

```
VITE_API_URL=http://localhost:8000/api
```

Для production используйте `.env.production`:

```
VITE_API_URL=https://your-api-domain.com/api
```

## Основные функции

### Для методиста

- ✅ Создание и редактирование курсов
- ✅ Загрузка текстовых материалов (TXT, DOCX, PDF)
- ✅ Автоматическая генерация конспектов с помощью AI
- ✅ Автоматическая генерация тестов с помощью AI
- ✅ Редактирование конспектов и вопросов тестов
- ✅ Публикация курсов
- ✅ Просмотр результатов студентов
- ✅ Экспорт результатов в CSV
- 🔒 Автоматическая анонимизация текста (в разработке)
- 🔒 OCR распознавание текста (в разработке)

### Для студента

- ✅ Просмотр доступных курсов
- ✅ Чтение конспектов
- ✅ Прохождение тестов
- ✅ Просмотр результатов тестирования

## Роли пользователей

Приложение поддерживает две роли:

1. **Методист** - создает курсы, генерирует контент, просматривает результаты
2. **Студент** - изучает курсы и проходит тесты

## API интеграция

Backend API эндпоинты:

- `POST /api/auth/login` - Авторизация
- `POST /api/auth/register` - Регистрация
- `GET /api/auth/me` - Получение текущего пользователя
- `GET /api/courses` - Список курсов
- `POST /api/courses` - Создание курса
- `PUT /api/courses/:id` - Обновление курса
- `DELETE /api/courses/:id` - Удаление курса
- `POST /api/courses/:id/generate-summary` - Генерация конспекта
- `POST /api/courses/:id/generate-test` - Генерация теста
- `POST /api/tests/:id/submit` - Отправка ответов на тест
- `GET /api/results` - Получение результатов

## Разработка

### Рекомендуемые IDE настройки

- [VSCode](https://code.visualstudio.com/)
- [Volar](https://marketplace.visualstudio.com/items?itemName=Vue.volar) (и отключите Vetur)
- [TypeScript Vue Plugin (Volar)](https://marketplace.visualstudio.com/items?itemName=Vue.vscode-typescript-vue-plugin)

### Линтинг и форматирование

```bash
# Проверка кода
npm run lint

# Автоматическое исправление
npm run lint:fix
```

### Type Checking

```bash
npm run type-check
```

## Основные компоненты

### Reusable UI компоненты

- `AppButton` - Кнопка с различными вариантами и состояниями
- `AppInput` - Поле ввода с поддержкой валидации
- `AppCard` - Карточка для отображения контента
- `AppModal` - Модальное окно

### Composables

- `useAuth()` - Работа с авторизацией
- `useApi()` - HTTP клиент с автоматической авторизацией
- `useToast()` - Уведомления

### Stores

- `authStore` - Управление авторизацией и пользователем
- `coursesStore` - Управление курсами
- `testsStore` - Управление тестами и результатами

## Лицензия

Proprietary

## Поддержка

Для вопросов и поддержки обращайтесь к команде разработки.
